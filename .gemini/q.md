`/src/app/layout/admin/Header/index.tsx` 파일에 로고는 Footer처럼 `/admin/webView/logo-settings`페이지에서 `푸터 로고/브랜드명 표시 설정` 부분처럼
`로고 + 브랜드명`이 클릭 되면 로고와 회사이름이 flex로 나란히 보일 수 있도록 해주고,
`로고`만 클릭되면, 로고만 보이고 회사이름은 안보이게 해주고
`브랜드명`만 클릭되면 로고는 안보이고 회사이름만 보이게 해줘
그러니까 회사이름도 삽입해서 푸터와 같이 해줘